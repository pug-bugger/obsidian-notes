---
date: 2026-05-01
tags:
  - DC
---

### Notes

```
import { translationStore } from "c/translationStore";
```

```
translationStore.sub(this.translationClb);
this.translationClb();

translationStore.unsub(this.translationClb);
```

```
translationGroup = "truckList";
  searchTruckHolderText = null;
  translationClb = () => {
    const translations = translationStore.getGroup(this.translationGroup) ?? {};
    this.searchTruckHolderText = translations.truck_label ?? "Search Trucks:";
  }
```

### Task
[RT-890 konsolės multilanguage realizavimas](https://bcline.lightning.force.com/lightning/r/a0NSZ00000MTnlR2AT/view)
- Fixing comments.
- Writing action type page store subscription logic.
*stand-up meet*
- Fixed code review comments. 
- Try to find custom metadata csv import solution. 
- Try import translations with cvs via cli command. 
- Import with cli works, but now on vscode terminal, need to write documentation how to use it in macbook terminal.
- Writing initial file csv of translations and implementing translated text display in trip list and planner. 
- Translate trip list component.
- trip list almost translated, remains translate labels in task rows and task details comsponents.